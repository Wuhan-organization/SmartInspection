package com.whut.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.whut.smartinspection.model.WholePatrolCard;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "WHOLE_PATROL_CARD".
*/
public class WholePatrolCardDao extends AbstractDao<WholePatrolCard, Long> {

    public static final String TABLENAME = "WHOLE_PATROL_CARD";

    /**
     * Properties of entity WholePatrolCard.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property PatrolHeadPageId = new Property(1, String.class, "patrolHeadPageId", false, "PATROL_HEAD_PAGE_ID");
    }

    private DaoSession daoSession;


    public WholePatrolCardDao(DaoConfig config) {
        super(config);
    }
    
    public WholePatrolCardDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"WHOLE_PATROL_CARD\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"PATROL_HEAD_PAGE_ID\" TEXT);"); // 1: patrolHeadPageId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"WHOLE_PATROL_CARD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, WholePatrolCard entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String patrolHeadPageId = entity.getPatrolHeadPageId();
        if (patrolHeadPageId != null) {
            stmt.bindString(2, patrolHeadPageId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, WholePatrolCard entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String patrolHeadPageId = entity.getPatrolHeadPageId();
        if (patrolHeadPageId != null) {
            stmt.bindString(2, patrolHeadPageId);
        }
    }

    @Override
    protected final void attachEntity(WholePatrolCard entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public WholePatrolCard readEntity(Cursor cursor, int offset) {
        WholePatrolCard entity = new WholePatrolCard( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // patrolHeadPageId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, WholePatrolCard entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPatrolHeadPageId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(WholePatrolCard entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(WholePatrolCard entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(WholePatrolCard entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
